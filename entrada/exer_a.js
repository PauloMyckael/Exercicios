// Lê os dados de entrada
let num = Number(prompt("Número: "));

let anterior = (num - 1);
let posterior = (num + 2);

// conclusão

alert("Número anterior: " + anterior + ", Número posterior: " + posterior);